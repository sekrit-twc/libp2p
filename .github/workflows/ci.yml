name: CI

on:
  push:
    branches:
      - 'master'
  pull_request:

jobs:
  run-tests:
    strategy:
      matrix:
        include:
          - os: ubuntu-24.04-arm
            compiler: g++
            cxx: g++
          - os: ubuntu-24.04-arm
            compiler: clang++
            cxx: clang++
          - os: ubuntu-latest
            compiler: g++
            cxx: g++
          - os: ubuntu-latest
            compiler: clang++
            cxx: clang++
          - os: windows-latest
            compiler: msvc
            cxx: cl
          - os: windows-latest
            compiler: clang-cl
            cxx: clang-cl
          - os: windows-11-arm
            compiler: msvc
            cxx: cl
          # TODO: Add icx when this issue is fixed: https://github.com/mesonbuild/meson/issues/14089

    name: "Test (${{ matrix.os }}, ${{ matrix.compiler }})"
    runs-on: ${{ matrix.os }}
    env:
      CXX: ${{ matrix.cxx }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Meson
        run: pip install meson

      - name: Set SIMD option
        run: |
          if [[ "${{ matrix.os }}" == *"arm"* ]]; then
            echo "MESON_SIMD_FLAG=-Dsimd=disabled" >> $GITHUB_ENV
          else
            echo "MESON_SIMD_FLAG=-Dsimd=enabled" >> $GITHUB_ENV
          fi
        shell: bash

      - name: Configure with Meson
        run: meson setup builddir --buildtype=release $MESON_SIMD_FLAG -Dtest=enabled ${{ matrix.compiler == 'msvc' && '--vsenv' || '' }}

      - name: Build
        run: meson compile -C builddir

      - name: Run tests
        run: meson test -C builddir --print-errorlogs
